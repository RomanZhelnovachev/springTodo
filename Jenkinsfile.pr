pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build Verification') {
            steps {
                bat 'mvn clean install -DskipTests'
            }
        }
    }
}